# Wiener sauasge made from WASM w/ Rust

## Requirements

- wasm-pack
  ```sh
  cargo install wasm-pack
  ```
- parcel
  ```sh
  npm i -g parcel-bundler
  ```

## Building
```sh
yarn install
parcel index.html --port 1234
```
